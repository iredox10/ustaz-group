<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../css/home.css">
    <title>admin</title>
</head>
    <style>
        .container{
            padding: 1rem;
            max-width: 800px;
            margin: 0 auto;
        }
         a:not(header ul li a),button{
            text-decoration: none;
            text-transform: capitalize;
            background: rebeccapurple;
            padding: .5rem;
            color: white;
            border-radius: .5rem;
            display: inline-block;
            margin: .3rem 0;
        }
        a:hover:not(header ul li a){
            background: rgb(19, 14, 24);
        }
        button{
            width: 100%;
            border: none;
            background: rgb(141, 14, 14);
            cursor: pointer;
        }
        button:hover{
            background: rgb(195, 32, 32);
        }
        table{
            padding: 0;
            table-layout: fixed;
        }
        th,td{
            border: 1px solid #000;
            padding: .5rem;
        }
        .top{
            display: flex;
            align-items: center;
            justify-content: space-around;
            margin: 1rem 0;
        }
    </style>
<body>
    <%- include('./partial/header.ejs') %> 
    <div class="top">
    <h1>admin page</h1>
    <a href="/employees-page">veiw staff page</a>
    </div>
    <div class="container">
        <h1>users registered</h1>
        
    <% users.forEach(user => { %>
        <table>
            <tr>
                <th>full name</th>
                <th>email</th>
                <th>phone number</th>
                <th>address</th>
                <th>password</th>
                <th>view/ delete</th>
            </tr>
            <tr>
                <td><%= user.name %> </td>
                <td><%= user.email %> </td>
                <td><%= user.phoneNumber %> </td>
                <td><%= user.address %> </td>
                <td><%= user.password %> </td>
                <td>
                    <a href="/user/<%= user.id %> ">view user</a>
            <form action="/admin/<%= user.id%>?_method=DELETE" method="POST">
                <button>delete</button>
            </form></td>
            </tr>
        </table>
        
        
  <%  }) %> 
</div>

</body>
</html>